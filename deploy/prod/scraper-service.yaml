apiVersion: v1
kind: Service
metadata:
  name: scraper
  namespace: microservices
spec:
  type: ClusterIP
  selector:
    app: scraper
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
