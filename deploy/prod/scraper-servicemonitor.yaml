apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: scraper
  namespace: microservices
  labels:
    app: scraper
spec:
  selector:
    matchLabels:
      app: scraper
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
